# data_root: /data/anpei/facial-data/seg_celebA_new/segmap    # 68 root
data_root: /data/new_disk2/liury/data/facial-data/seg_teaser  # docker servers
# checkpoint_path: ./checkpoints/face_seg_20_0618.pth    # Path to the pre-trained checkpoint
checkpoint_path: ./checkpoints/061721face_seg_800.pth    # Path to the pre-trained checkpoint
overwrite_embeddings: True
data_type: seg
opt_cam: True
no_validation: True
freeze_networks: True
img_sidelengths: 128
output_sidelength: 128
batch_size_per_img_sidelength: 8
max_steps_per_img_sidelength: 200000
out_channels: 20
steps_til_reset: 20000
steps_til_ckpt: 1000